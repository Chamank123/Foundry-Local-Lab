# Local training environment (rebuild with requirements_nn.txt)
.venv/

# Received datasets — the sender already holds these, and the handoff says to
# keep received data unchanged. Extract/receive them here to reproduce a run.
pilot_shards/
overnight_shards_8m/

# Raw Lichess sample used for prep (not shipped)
pilot_raw.jsonl

__pycache__/
*.pyc
